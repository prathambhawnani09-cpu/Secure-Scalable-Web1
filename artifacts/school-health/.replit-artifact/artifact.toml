kind = "web"
previewPath = "/"
title = "SchoolHealth AI"
version = "1.0.0"
id = "artifacts/school-health"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25577

[services.development]
run = "pnpm --filter @workspace/school-health run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/school-health", "run", "build" ]
publicDir = "artifacts/school-health/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25577"
BASE_PATH = "/"
